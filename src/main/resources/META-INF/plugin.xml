<idea-plugin>
    <!-- 插件id，不可重复，必须唯一。插件的升级后续也是依赖插件id来进行识别的 -->
    <id>com.github.hollis.live.templates.plus.apache.commons</id>

    <!--  插件名称 -->
    <name>Live Templates Plus(apache-commons)</name>

    <!-- 插件开发人员，这里写一下开发者的个人信息. -->
    <vendor>Hollis</vendor>

    <!--  插件描述，这里一般是写插件的功能介绍啥的 -->
    <description><![CDATA[
        Based on the live template enhancement plugin of [org.apache.commons.commons-lang3] and [org.apache.commons.commons-collections4].
        基于 org.apache.commons.commons-lang3 和 org.apache.commons.commons-collections4 的 live template 增强插件
    ]]></description>

    <!--  插件依赖，这里我们默认引用idea自带的依赖即可  -->
    <depends>com.intellij.modules.platform</depends>

    <!-- 定义拓展点，用于拓展其他人插件功能拓展点， 或者 IntelliJ 平台核心功能 -->
    <extensions defaultExtensionNs="com.intellij">
        <defaultLiveTemplates file="/liveTemplates/Java.xml"/>
        <liveTemplateContext implementation="com.github.hollis.JavaContext" contextId="JAVA"/>
    </extensions>
</idea-plugin>

